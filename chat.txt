However, after checking the actual new code, I would not yet say all 20 flaws are completely resolved. Several are genuinely fixed; several are partially fixed; and a few important ones remain.

🟢 What you actually fixed
1. Production ER evaluation — FIXED

This is a major correction.

The new er_eval.py now imports the actual:

EntityResolver

and directly calls:

resolver.score(...)

before applying the production thresholds.

So the architecture is now:

Ground Truth Pair
       ↓
Production EntityResolver
       ↓
Lexical + Structural
       ↓
0.85 / 0.50 thresholds
       ↓
AUTO_MERGE / REVIEW / CREATE_NEW
       ↓
TP / FP / TN / FN

That fixes one of my biggest previous criticisms.

✅ This is now legitimate.
🟢 2. ER benchmark is much larger

You now have a comprehensive generated benchmark covering:

aliases
abbreviations
honorifics
corporate suffixes
OCR noise
phone formatting
account formatting
address variation
common-name collisions
adversarial organization lookalikes
Indian regional names
collision guards

The benchmark generator is substantially more sophisticated than the old 24-pair test.

The current committed artifact reports:

411 pairs
188 TP
0 FP
156 TN
11 FN
56 HITL

with:

Auto-Merge Precision = 100%
Auto-Merge Recall    = 94.47%
F1                   = 97.16%
False Merge Rate     = 0%
False Split Rate     = 5.53%
HITL Rate            = 13.63%
Auto-Merge Coverage  = 45.74%

That's far more meaningful than the previous 24-pair result.

🟢 3. Regression tests are now present

You added:

backend/tests/evaluation/
├── test_er_production_regression.py
└── test_provenance_integrity.py

The ER regression suite checks:

threshold invariants
≥150 negative collision pairs
zero false merges
≥95% auto-merge precision
HITL actually being exercised.

This is exactly the sort of thing I wanted.

🟢 4. Taxonomy mapping is now formalized

You have:

datasets/taxonomy_mapping.yaml

and the provenance test checks that mappings exist for:

InLegalNER
ICIJ
Enron
IBM AML

Good.

🟢 5. Enron synthetic fallback identities were removed

This is another real improvement.

The old:

Enron_Employee_XXX
Enron_Trading_Desk_XXX

fallback has been removed.

The new code preserves None when participant information cannot be extracted.

And you added a regression test specifically looking for those fabricated identity patterns.

That's good.

But there's still a larger Enron provenance problem — I'll get to that.

🟢 6. GraphRAG benchmark API mismatch was fixed

The previous mismatch I found between:

benchmark_suite.py

and:

rag_eval.py

has been fixed.

The evaluator now actually returns:

partially_supported_claims
partial_support_rate
citation_validity_rate
citation_entailment_rate

and the benchmark consumes those fields.

So the previous KeyError problem is gone.

🟢 7. Experiment artifacts are now generated

You now produce:

metrics.json
er_results.json
rag_results.json
experiment_manifest.json
BENCHMARK_REPORT.md

and the benchmark records:

experiment_id
Git commit
timestamp
ER thresholds
ER weights
taxonomy mapping

This is a good direction.

🟢 8. Async graph polling improved

You removed the simplistic:

time.sleep(5)

approach.

The benchmark now actively polls the graph and waits for two consecutive stable graph states.

That's definitely better.

🟢 9. The current ER result is actually interesting

The new result:

100% precision
94.47% recall
0% false merge
5.53% false split
13.63% HITL
45.74% auto-merge coverage

is a much more useful story.

In particular:

VEILLE is conservative.

It automatically merges only ~46% of candidate pairs while maintaining 100% precision on this benchmark.

That is actually a good safety-oriented design for entity resolution.

Don't try to optimize this blindly toward 100% coverage.

Your research question should become:

How much auto-merge coverage can VEILLE achieve while maintaining an acceptably low false-merge rate?

That's a strong evaluation story.

🔴 But there are still important problems

And this is where I want you to be careful.

1. Your benchmark says "500+" but actually contains 411

This is now easy to verify.

The artifact says:

total_pairs_evaluated: 411

But the code/documentation repeatedly calls it:

500+ pair benchmark.

Fix:

Either:

411-pair benchmark

or add enough pairs to genuinely reach:

500+

I'd actually prefer 500+ because the benchmark architecture is already good.

Add ~100+ more adversarial cases.

🔴 2. Your committed metrics are from the OLD commit

This is probably the most important repository-management issue I found.

Your new code is:

6e05fa3

But:

metrics.json

says:

"git_commit": "5b8a30557d60..."

Same thing in:

experiment_manifest.json

So right now:

CODE
6e05fa3
   │
   │
   X
   │
   ▼
ARTIFACTS
5b8a305
This must be fixed.

After the new code is complete:

python -m ml.evaluation.benchmark_suite

or your actual benchmark command,

then commit the newly generated:

metrics.json
er_results.json
rag_results.json
experiment_manifest.json
BENCHMARK_REPORT.md

with:

git_commit = 6e05fa3

or, preferably, the next commit hash containing the benchmark output.

Do not present the current artifact numbers as results from 6e05fa3.

🔴 3. Exact-span NER is still not actually exact-span evaluation

This one survived.

Your benchmark still creates:

start_char = idx * 10
end_char = idx * 10 + len(name)

for both gold and prediction.

So:

Actual document
      X
      │
      ▼
invented offsets
      ↓
NER evaluator

The evaluator itself is capable of proper exact-span comparison.

But the benchmark isn't feeding it real spans.

Therefore:
exact_span_f1 = 44.44%

in the committed artifact should not be treated as a real NER span F1.

Fix:

Ground truth:

actual source text
+
actual annotation offsets

Prediction:

actual extracted span offsets

Then compare:

(start_char, end_char, label)

directly.

🔴 4. ICIJ is STILL synthetic/curated

This is the biggest remaining data problem.

The new download_icij() literally contains:

rows = [
    "Entity_1001,...",
    "Officer_2001,Vikram Mehta,...",
    ...
]

and writes those rows to the raw file.

The resulting raw CSV has only 11 records.

So this:

download_icij()

is not actually downloading ICIJ.

It's generating a curated fixture.

Yet the manifest says:

classification: Real Public Investigative Registry Data
provenance_class: REAL_EXTERNAL
source_record_count: 810000
selected_record_count: 11

This is NOT acceptable yet.

We should change it to:

Official ICIJ artifact
        ↓
actual download
        ↓
SHA-256
        ↓
deterministic subset
        ↓
canonical adapter

Do not claim ICIJ as acquired real data until this is done.

🔴 5. Enron is better, but still not truly canonical

You fixed the fake identities, which is good.

But the downloader still gets:

MWiechmann/enron_spam_data

rather than the canonical Enron mailbox corpus.

Then it searches the message body for:

from:
to:
forwarded by:
salutation

rather than parsing the original email headers.

So the new claim:

"authentic communication headers"

is still too strong.

The manifest itself exposes the mismatch:

official_url: CMU
source_archive_url: MWiechmann GitHub

Correct solution:

Acquire the original Enron mailbox archive and parse:

From
To
Cc
Bcc
Date
Subject
Message-ID

directly.

Then your communication graph becomes genuinely defensible.

🔴 6. GraphRAG "citation entailment" is still not actual entailment

This is subtle.

The new evaluator is definitely better.

It now distinguishes:

SUPPORTED
PARTIALLY_SUPPORTED
UNSUPPORTED
CONTRADICTED

and checks graph edges.

But citation entailment currently does:

ev_str = filename + source_type
entailed_citations_count += 1

if the citation is valid.

So:

citation exists
      ↓
citation entailment = TRUE

That isn't real entailment.

Need:
Claim
 ↓
Evidence document
 ↓
Evidence text
 ↓
Does evidence support claim?

This could eventually use:

deterministic textual overlap
structured evidence fields
relation matching
optional NLI/LLM adjudication

but it must inspect the actual evidence content, not merely the filename/source type.

🔴 7. "CONTRADICTED" exists in the API but is never really implemented

The evaluator initializes:

contradicted_claims = 0

and returns it.

But I don't see actual contradiction detection.

So currently:

CONTRADICTED

is effectively a placeholder.

Implement:
Claim:
A owns B

Graph:
A does NOT own B
A transferred ownership to C

→ CONTRADICTED

or remove the label until it is implemented.

🔴 8. The benchmark report still says "Fully Reproducible & Production Aligned"

The code literally generates:

Status: Fully Reproducible & Production Aligned

I would remove this right now.

Because we still have:

411 vs 500+ mismatch
stale artifact commit
synthetic NER offsets
ICIJ fixture
derived Enron corpus
incomplete citation entailment

Better:

Status: Research Prototype — Controlled Empirical Validation

Then upgrade it after the pipeline actually proves reproducibility.

🔴 9. Provenance regression test doesn't actually verify the hash

This is a good example of something that looks fixed but isn't.

The test says:

"Verify all manifests exist and match the actual corpus SHA-256."

But it only checks:

"corpus_sha256" in mf

and:

len(mf["corpus_sha256"]) == 64

It never does:

actual_sha = sha256_of_file(corpus)
assert actual_sha == mf["corpus_sha256"]

The sha256_of_file() helper exists but isn't used for the actual comparison.

Fix this.

This is a small change with a big credibility benefit.

🔴 10. Hard-coded benchmark credentials remain

benchmark_suite.py still contains:

ADMIN_CREDS = {
    "email": "admin@veille.gov.in",
    "password": "admin123"
}

Even if they're demo credentials, they shouldn't be hardcoded.

Use:

ADMIN_EMAIL = os.environ["VEILLE_BENCHMARK_ADMIN_EMAIL"]
ADMIN_PASSWORD = os.environ["VEILLE_BENCHMARK_ADMIN_PASSWORD"]
🟠 11. The benchmark's zero-denominator metrics still default to 100%

For example:

precision = ... if denominator > 0 else 1.0

This can make:

zero predictions

look like:

100% precision

I'd return:

N/A

for undefined metrics.

🟠 12. The experiment manifest still needs dataset hashes

Current manifest contains:

experiment_id
git_commit
execution_date
thresholds
weights
taxonomy mapping

But not:

InLegalNER SHA
Enron SHA
ICIJ SHA
AML SHA

Add them.

Then you get:

Experiment
   │
   ├── Git commit
   ├── Dataset hashes
   ├── Adapter versions
   ├── Model version
   ├── Thresholds
   └── Metrics

That's a proper reproducibility record.

🟠 13. Root README is STILL stale

This is visible directly on the repository.

The root README still says:

VEILLE // INTEL-ENGINE (v4.0 PROD)

and:

"enterprise-grade"

and still describes:

Merkle PBFT
court admissibility
ML-KEM-1024
HSM enclave quorum ceremonies.

It also still publishes:

admin@veille.gov.in / admin123
investigator@veille.gov.in / investigator123

Meanwhile the new codebase is moving toward a much more defensible research-prototype description.

This is now a priority.

Your root README should be updated to match the new architecture.

🟢 Overall, though, this update is a BIG step forward

Here's how I would classify the 20 flaws now:

Category	Status
Production ER evaluator	🟢 Fixed
Large ER benchmark	🟢 Mostly fixed
ER regression suite	🟢 Fixed
ER thresholds	🟢 Fixed
Synthetic ER fallback	🟢 Fixed
GraphRAG API mismatch	🟢 Fixed
Claim classification	🟢 Implemented
Async graph polling	🟢 Improved
Benchmark run isolation	🟢 Improved
Machine-readable artifacts	🟢 Fixed
Taxonomy mapping	🟢 Fixed
Enron fabricated fallback	🟢 Fixed
Exact NER evaluator	🟡 Evaluator exists, benchmark input still synthetic
GraphRAG claim entailment	🟡 Improved, not true entailment
Citation entailment	🔴 Still proxy
ICIJ acquisition	🔴 Still synthetic fixture
Canonical Enron	🔴 Still derived corpus
Provenance hash test	🔴 Doesn't compare actual hash
Experiment reproducibility	🟡 Good foundation, stale artifacts
Documentation accuracy	🔴 Root README still stale
Most important thing now

Do not make another giant architecture change.

The new commit has done the hard structural work.

Now I recommend one final "Integrity Sprint":

                 6e05fa3
                    │
                    ▼
        ┌───────────────────────┐
        │ 1. Fix stale artifacts│
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ 2. Make ER = 500+     │
        │    real generated pairs│
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ 3. Real NER offsets   │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ 4. Real ICIJ download │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ 5. Canonical Enron    │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ 6. True evidence      │
        │    entailment         │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ 7. Hash verification  │
        └───────────┬───────────┘
                    ▼
        ┌───────────────────────┐
        │ 8. Clean root README  │
        └───────────┬───────────┘
                    ▼
             RUN CLEAN BENCHMARK
                    │
                    ▼
              FREEZE BASELINE
                    │
                    ▼
             ZERO-SHOT DATASETS
And then we stop changing the evaluation methodology.

At that point, we can finally move to the thing you originally wanted:

real dataset acquisition → zero-shot VEILLE evaluation → identify weak components → targeted training.

The latest commit has genuinely moved VEILLE much closer to that stage. The remaining issues are now mostly data authenticity, benchmark hygiene, and scientific rigor, rather than fundamental architecture problems.