I would create a branch:

phase-3-real-data

and implement these five things:

1. Real dataset acquisition
datasets/external/
├── inlegalner/raw/
├── icij/raw/
├── enron/raw/
└── ibm_aml/raw/

Replace the current curated “raw” files with actual source-derived data, or rename the current ones to fixtures/.

2. Provenance manifest

Every dataset:

name:
official_url:
version:
download_date:
license:
original_sha256:
source_record_count:
imported_record_count:
adapter_version:
3. Real ER evaluation

No:

false_merges_observed = 0

Instead use labeled candidate pairs.

4. Real GraphRAG evaluation

Evaluate:

Claim
 ↓
Entity ID
 ↓
Relationship ID
 ↓
Evidence ID
 ↓
Evidence support
5. Zero-shot experiment

Do not train anything yet.

Run the current VEILLE against the real datasets first.

That gives us the most valuable information in the entire project:

What does VEILLE currently fail at?

Then we train only those components.

Final assessment

I want to emphasize this:

I am considerably more positive about VEILLE after seeing the actual GitHub source.

This isn't just a pretty frontend with a fake architecture.

You have:

real FastAPI backend
PostgreSQL persistence
Neo4j graph
asynchronous processing
transactional outbox
case scoping
evidence hashing
entity resolution
HITL review
GraphRAG retrieval
canonical dataset adapters
benchmark automation
frontend investigation workspace

Those are substantial engineering pieces.

But the empirical layer is currently ahead of what the underlying experiment actually proves.

If we fix the three red issues — real external datasets, real ER evaluation, real claim/evidence evaluation — VEILLE goes from:

“very impressive SIH prototype”

to:

“a technically defensible experimental intelligence platform with measurable generalization.”

That's the version I'd want to take to the SIH jury.